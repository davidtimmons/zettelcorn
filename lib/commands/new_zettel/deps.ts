/**
 * External Deno dependencies for this command module. Uses indirection to help decouple this
 * module so it can be moved outside the commands umbrella if needed.
 * @protected
 * @module commands/new_zettel/deps
 * @see module:commands/deps
 */

export * from "../deps.ts";
